# SQL Lite db name
db.name=MyOrders.db

# DB creation script
ddl.script=META-INF/MyOrders.sql

# Location of XML mapping file in classpath
persistence.mapping.xml=META-INF/persistence-mapping.xml

# Usage tracking
tracking.enabled=false
